import React from "react";

const IconWrapper = ({ children }) => (
  <div className="w-8 h-8 bg-[#007a6e] rounded-full flex items-center justify-center text-white">
    {children}
  </div>
);

export default IconWrapper;
